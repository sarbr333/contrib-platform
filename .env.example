# Neon Pooled 连接串（host 带 -pooler），运行时用，Vercel 上也填这个
DATABASE_URL=

# Neon Direct 连接串（host 不带 -pooler），Prisma migrate/db push 用
DIRECT_URL=

# NextAuth 密钥，随便一个长字符串。生成：openssl rand -base64 32
AUTH_SECRET=

# 你自己的邮箱，第一次注册后自动成为 owner
OWNER_EMAIL=
